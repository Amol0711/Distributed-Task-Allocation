# Application evaluation

These tables contain paired retention, bootstrap intervals, certificate
utilizations, descriptive curvature, and covariance diagnostics for 32 evaluation
seeds per application. `scripts/build_application_evidence.py` reconstructs the
tables from complete validated campaign records. Each policy's exact-score-greedy
comparison is evaluated along that policy's own residual-resource trajectory.
